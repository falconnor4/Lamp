[package]
name = "genie-infer"
version = "0.1.0"
edition = "2021"

[dependencies]
safetensors = "0.4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokenizers = { version = "0.20", default-features = false, features = ["onig"] }
rand = "0.8"